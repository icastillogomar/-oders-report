{
  "name": "reporte-pedidos-lp",
  "version": "1.0.0",
  "description": "Dashboard de Reporte de Pedidos LP",
  "scripts": {
    "dev:backend": "go run backend/cmd/api/main.go",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:full": "concurrently \"go run backend/cmd/api/main.go\" \"npm run dev:frontend\"",
    "deploy": "gcloud run deploy reporte-pedidos-lp --source . --region us-central1 --allow-unauthenticated --project fechaestimadaentregaprod --set-env-vars=\"GCP_PROJECT_ID=fechaestimadaentregaprod,BQ_LOCATION=US\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}